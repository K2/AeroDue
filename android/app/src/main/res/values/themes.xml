<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AeroDue" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Launch theme: branded splash that hands off to the app theme. -->
    <style name="Theme.AeroDue.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.AeroDue</item>
    </style>
</resources>
